<h1>Veuillez créer votre restaurant</h1>

<%= form_for @resto do |f| %>
  <p> <%= f.label :name %> <%= f.text_field :name%> </p>
  <p> <%= f.label :description %> <%= f.text_area :description %> </p>
  <p> <%= f.label :picture_url %> <%= f.text_field :picture_url %> </p>
  <p> <%= f.submit "Create" %> </p>
<% end %>

<%= link_to 'Back', restos_path %>